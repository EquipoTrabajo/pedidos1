<div class="container">
  <div class="panel panel-default">
    <div class="panel-heading">
      <h3 class="panel-title">{{ office.name }}</h3>
    </div>
    <div class="panel-body">
      <table class="table table-bordered">
        <thead class="bg-info">
          <th>ID</th>
          <th>Dirección de Entrega</th>
          <th>Fecha</th>
          <th>Estado</th>
        </thead>
        <tbody>
          {{#each office.wip.orders}}
            <td>{{ _id }}</td>
            <td>{{ address_deliver.name }}</td>
            <td>{{ created_at }}</td>
            <td>{{ state.name }}</td>
          {{/each}}
        </tbody>
      </table>
    </div>
  </div>
</div>